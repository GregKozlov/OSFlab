import React from 'react';
import Styled from '@oracle-cx-commerce/react-components/styled';

import css from './styles.css';

const PageViewSubscriber = props => {
  return (
    <Styled id="PageViewSubscriber" css={css}>
      <div className="PageViewSubscriber">PageViewSubscriber</div>
    </Styled>
  );
};

export default PageViewSubscriber;
